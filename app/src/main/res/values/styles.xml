<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="button_style" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for buttons. -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:maxWidth">@dimen/buttonMaxWidth</item>
        <item name="android:minWidth">@dimen/buttonMinWidth</item>
    </style>
    <style name="score_style" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for player scores. -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">@dimen/player_scores</item>
    </style>
    <style name="player_name_style" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for player scores. -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">@dimen/player_scores</item>
        <item name="android:textColor">@color/dark_gray</item>
    </style>




</resources>
